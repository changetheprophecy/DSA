#include<bits/stdc++.h>
using namespace std;
int main()
{
    string n,rev;
    cout<<"Enter the string: ";
    cin>>n;
    rev = n;
    reverse(rev.begin(), rev.end());
    if(rev==n)
        cout<<"Palindrome"<<endl;
    else
        cout<<"Not palindrome"<<endl;
}
